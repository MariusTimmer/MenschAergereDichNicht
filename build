##
# This script creates a runnable jar-file
##

MANIFEST=META-INF/MANIFEST.MF
JAR_FILE=MADN.jar

# Before the jar will be created the sources have to be "compiled"
./clear
make

jar cfm $JAR_FILE $MANIFEST $(find . -name "*.class") $(find . -name "*.java")